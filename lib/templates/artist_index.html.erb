<!DOCTYPE html>
<html>
  <head>
    <title>Artist Index</title>
  </head>
  <body>
    <h1>All Artists</h1>
      <%= artists.each_with_index do |artist, i|%>
        <li><a href="_site/artist/<%= artist.url %>"><%= i+1 %><%= artist.name %></a></li>
      <% end %>
  </body>
</html>